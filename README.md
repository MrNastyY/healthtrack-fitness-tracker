# HealthTrack Frontend

This is the React-based frontend for the HealthTrack fitness tracking system.

It connects to the Django REST API backend and provides:
- Login & Register pages
- Dashboard with activity summary
- Activity CRUD (Add, Edit, Delete)
- Toast notifications and modern UI with TailwindCSS

## Scripts
- `npm start` – Start local development
- `npm run build` – Build production files
